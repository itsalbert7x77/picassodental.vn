/**
 * Seed script - runs INSIDE Strapi to import content
 *
 * Usage:
 *   cd backend
 *   npx ts-node -e "require('./scripts/seed')"
 *
 * OR: Copy the bootstrap function into backend/src/index.ts temporarily
 */

// This script creates a standalone Strapi instance to seed data
// Run from the backend directory: node -e "require('@strapi/strapi').default().start().then(() => {})"

export {};
